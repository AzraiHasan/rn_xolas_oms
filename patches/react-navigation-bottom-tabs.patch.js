// This file provides patches/fixes for the bottom tabs navigation
// to resolve shadow property deprecation warnings

// In case we need to manually patch the @react-navigation/bottom-tabs package,
// we can use the patch-package utility to create patches.

// To install: npm install patch-package --save-dev
// Create patch: npx patch-package @react-navigation/bottom-tabs
// Apply patch: npx patch-package

// For now, we're using our custom styles in the layout component to override defaults.
